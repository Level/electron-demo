{
  "name": "electron-demo",
  "private": true,
  "dependencies": {
    "electron": "1.7.5",
    "level": "^2.0.1"
  },
  "scripts": {
    "postinstall": "cd node_modules/leveldown && npm run install --target=1.7.5 --runtime=electron",
    "start": "electron .",
    "test": "standard"
  },
  "devDependencies": {
    "standard": "^10.0.3"
  }
}
